String类型
	equals()方法
		只能调用引用类型对象（应用对象的对应数值+地址值是否相同）
		比较引用变量的数值是否相等
		练习：Order.java
	toString()方法
		返回对象所在的类，以及对象对应的堆空间实体的首地址

包装类（Wrapper）
	针对八中基本数据类型定义相应的引用类型——包装类
		int--interger    char--Character
	包装类的使用
		包装类：八中基本数据类型对应着一个类（基本数据类型+包装类+String之间的相互转化）
		练习：包装类+String+基本数据类型之间的相互转换 TestWrapper.java

	Junit单元测试类
		 * 1.当前工程下-右键build path-add libraries-Junit4
		 * 2.在主类中,创建一个空参的无返回值的方法，（如：public void test1()）用于代码的测试，方法上声明：@Test
		 * 3.导入import org.junit.Test;
		 * 4.方法名1()的方法中，进行代码的编写。
		 * 5.测试：双击方法名，右键run as-junit Test即可。
		 练习：TestJuint.java

	关键字static
		由类创建的所有对象，都公用这一个属性
		当其中的一个对象对此属性进行修改，会导致其他对象对此属性的一个调用时其数值改变
		类变量是随着类的加载而加载的，而且独一份：该静态变量可以直接被未实例化的类调用
		类变量存在于静态域中
		static修饰的方法（类方法）
			随着类的加载而加载，独一份
			可以通过类.方法的方式来调用
			内部可以调用静态的属性或方法，而不能调用非静态的属性和方法（即静态不用实例化就可以直接调用）
			静态的生命周期长于非静态，回收时晚于非静态
			静态的方法中不可以有this（隐含有对象）+super（隐含有继承）关键字


		
